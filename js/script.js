const inputName = document.getElementById("nameComplete");
const age = document.getElementById("age");
const button = document.getElementById("calculate");

button.addEventListener("click", function (e) {
  e.preventDefault();
  
});
